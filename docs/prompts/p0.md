# P0 审查提示词（最高验证强度）

## 审查上下文

- 验证等级：{validation_level}
- 策略：{plan_prefix}
- 审查文件：{review_files}
- 语言范围：{scopes}

## 审查要求（必须全部满足）

1. 只要存在安全、并发、数据一致性、权限边界问题，直接判为高危。
2. 输出必须包含：问题证据、影响范围、修复路径、回归测试点。
3. 结果必须给出明确的阻断建议（可发布/不可发布）。
4. 未确认的风险按“疑似高危”处理，不能降级为低风险。

## 语言专项规则

{language_rules}
